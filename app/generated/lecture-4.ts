// Generated from content/lectures/lecture-4.qmd.
// Run `npm run content:render` after editing the QMD source.
export const lecture4Html = "<header id=\"title-block-header\">\n<h1 class=\"title\">Lecture 4 (draft): Extraction, language identification, and quality filtering</h1>\n\n</header>\n\n\n<p>An acquired object is not yet a training document. HTML contains navigation and templates, PDFs encode layout rather than reading order, repositories contain generated files and vendored dependencies, and Unicode permits several representations of visually similar text. Extraction determines which of these structures become model input.</p>\n<h2 id=\"learning-objectives\">Learning objectives</h2>\n<p>By the end of this lecture, you should be able to:</p>\n<ol type=\"1\">\n<li>design separate extraction paths for web, PDF, and code sources;</li>\n<li>normalize text without silently destroying meaningful structure;</li>\n<li>evaluate language identification on multilingual and code-switched data;</li>\n<li>compare heuristic, classifier-based, safety, and PII filters; and</li>\n<li>measure the precision, recall, retention, and distribution shift produced by a filter.</li>\n</ol>\n<h2 id=\"extraction-is-a-model-facing-decision\">Extraction is a model-facing decision</h2>\n<p>A web extractor should distinguish main content from menus, cookie notices, related links, comments, and repeated headers. A PDF extractor must reconstruct reading order, headings, tables, equations, and page boundaries. A code extractor needs repository metadata, language, file paths, license boundaries, and rules for generated or minified content.</p>\n<p>The output should preserve both text and structure:</p>\n<pre class=\"text\"><code>document_id       source_id        extractor_version\ncontent           content_type     language_scores\nsegments          structural_tags  quality_features\nsource_offsets    warnings         transformation_log</code></pre>\n<p>Source offsets make it possible to inspect the original object when a filter fails. Warnings preserve uncertainty rather than converting every parse into apparently clean text.</p>\n<h2 id=\"unicode-normalization-and-segmentation\">Unicode normalization and segmentation</h2>\n<p>Normalization can standardize whitespace, line endings, and canonically equivalent Unicode sequences. It should not transliterate scripts, erase mathematical notation, merge code indentation, or remove diacritics without a task-specific reason. Store raw content separately from normalized derivatives so the decision remains reversible.</p>\n<p>Document segmentation defines the unit used for filtering, deduplication, packing, and lineage. One very long page may become several coherent documents; one forum thread may retain a structured sequence of turns. Fixed-length chunks created only for model context should not replace the semantic document record.</p>\n<h2 id=\"language-identification\">Language identification</h2>\n<p>A language identifier returns a score or distribution, not ground truth. Short strings, named entities, transliterated text, related languages, dialects, and code-switching are common failure cases. An <code>unknown</code> or multilingual label can be more honest than forcing every document into one language.</p>\n<p>Evaluation uses a manually labeled hard-case set and reports a confusion matrix, precision, recall, and abstention by language. Aggregate accuracy can hide a system that performs well on English and poorly on Armenian. The intended sampling policy determines whether false positives or false negatives are more costly.</p>\n<h2 id=\"quality-filtering\">Quality filtering</h2>\n<p>Heuristic features may include document length, symbol ratios, line repetition, sentence structure, stopword patterns, or the fraction of boilerplate. A learned filter predicts quality from labeled examples or similarity to a reference distribution. <a href=\"https://arxiv.org/abs/2406.11794\">DataComp-LM</a> <span class=\"citation\" data-cites=\"li2024datacomplm\">(<span class=\"nocase\">Li et al.</span> 2024)</span> provides controlled comparisons of extraction, deduplication, filtering, and mixing choices, including a strong model-based filtering baseline. Its result is evidence for that experimental setting, not a universal proof that one score defines high-quality text.</p>\n<p>A filter changes the distribution twice: it removes documents, and it changes which sources and languages remain available for later sampling. Report:</p>\n<ul>\n<li>documents and tokens retained globally and by subgroup;</li>\n<li>precision and recall on manually labeled decisions;</li>\n<li>examples near the threshold;</li>\n<li>changes in length, language, domain, and time distributions; and</li>\n<li>a fixed-compute downstream ablation.</li>\n</ul>\n<h2 id=\"safety-and-privacy-policies\">Safety and privacy policies</h2>\n<p>Toxicity and PII detectors can remove a document, redact a span, or attach metadata for later sampling. The correct action depends on the threat model and release policy. Span replacement preserves surrounding text but introduces a synthetic token pattern. Whole-document removal avoids that pattern but may disproportionately remove some sources. Silent deletion without counts makes both effects impossible to audit.</p>\n<h2 id=\"studio-build-and-audit-the-pipeline\">Studio: build and audit the pipeline</h2>\n<p>Implement <code>ingest → extract → normalize → segment → language ID → quality score → policy action</code> for a small English–Russian–Armenian collection containing web pages, PDFs, and code. Manually label at least 30 hard cases before choosing thresholds.</p>\n<p>The report includes the confusion matrix, retention by language and source type, false-positive and false-negative examples, and one downstream proxy measurement. A successful pipeline is not the one that deletes the most; it is the one whose trade-offs are explicit and reproducible.</p>\n<h2 id=\"what-to-remember\">What to remember</h2>\n<ol type=\"1\">\n<li>Extraction determines which source structures become training evidence.</li>\n<li>Raw and normalized objects should remain separately recoverable.</li>\n<li>Language identification needs multilingual hard cases and an abstention policy.</li>\n<li>Quality, safety, and privacy filters encode different objectives.</li>\n<li>Every threshold needs subgroup retention, error analysis, and a downstream ablation.</li>\n</ol>\n<h2 class=\"unnumbered\" id=\"references\">References</h2>\n<div id=\"refs\" class=\"references csl-bib-body hanging-indent\" role=\"list\">\n<div id=\"ref-li2024datacomplm\" class=\"csl-entry\" role=\"listitem\">\n<span class=\"nocase\">Li, Jeffrey, Alex Fang, Georgios Smyrnis, et al.</span> 2024. <span>“<span>DataComp-LM</span>: In Search of the Next Generation of Training Sets for Language Models.”</span> <em>arXiv Preprint arXiv:2406.11794</em>. <a href=\"https://arxiv.org/abs/2406.11794\">https://arxiv.org/abs/2406.11794</a>.\n</div>\n</div>";
