// Generated from content/lectures/lecture-8.qmd.
// Run `npm run content:render` after editing the QMD source.
export const lecture8Html = "<header id=\"title-block-header\">\n<h1 class=\"title\">Lecture 8 (draft): Training systems and efficient pretraining</h1>\n\n</header>\n\n\n<p>A correct Transformer can still be impossible to train. Model parameters, optimizer states, activations, attention intermediates, communication, storage, and failures all compete for a finite systems budget. Training systems make the mathematical experiment executable without changing which scientific comparison is being claimed.</p>\n<h2 id=\"learning-objectives\">Learning objectives</h2>\n<p>By the end of this lecture, you should be able to:</p>\n<ol type=\"1\">\n<li>calculate parameter, gradient, optimizer-state, activation, and attention memory;</li>\n<li>explain AdamW, warmup, decay, global batch size, and gradient accumulation;</li>\n<li>distinguish data, tensor, pipeline, and fully sharded parallelism;</li>\n<li>explain the systems purpose of FlashAttention, activation checkpointing, ZeRO/FSDP, and mixture of experts; and</li>\n<li>design a restartable run with throughput, numerical, data, and fault diagnostics.</li>\n</ol>\n<h2 id=\"optimizer-and-schedule\">Optimizer and schedule</h2>\n<p>AdamW maintains moving averages of first and second moments in addition to parameters and gradients. In mixed precision, a training system may also retain higher-precision master weights. Optimizer memory can therefore exceed model-weight memory several times over.</p>\n<p>Warmup limits unstable early updates before the optimizer statistics and activations settle. A decay schedule controls later step size. The <strong>global batch</strong> combines microbatches across devices and gradient-accumulation steps before one optimizer update. Changing it changes the optimization regime, not only throughput.</p>\n<p>The run manifest records optimizer hyperparameters, schedule in tokens or steps, gradient clipping, weight decay exclusions, numerical precision, loss scaling, and the exact number of target tokens processed.</p>\n<h2 id=\"memory-accounting\">Memory accounting</h2>\n<p>Separate at least five categories:</p>\n<ol type=\"1\">\n<li>parameters;</li>\n<li>gradients;</li>\n<li>optimizer states and master weights;</li>\n<li>saved activations for backward; and</li>\n<li>temporary kernel and communication buffers.</li>\n</ol>\n<p>Activation memory grows with batch, sequence length, model width, and layers. A naive attention implementation additionally materializes a <math display=\"inline\" xmlns=\"http://www.w3.org/1998/Math/MathML\"><semantics><mrow><mi>T</mi><mo>×</mo><mi>T</mi></mrow><annotation encoding=\"application/x-tex\">T\\times T</annotation></semantics></math> score matrix per head. A model can fit at sequence length 2,048 and fail at 8,192 even when its parameter count is unchanged.</p>\n<p><strong>Activation checkpointing</strong> stores selected activations and recomputes the rest during backward, trading additional FLOPs for lower memory. The trade should be measured in peak memory and tokens per second rather than described only qualitatively.</p>\n<h2 id=\"kernels-and-arithmetic-intensity\">Kernels and arithmetic intensity</h2>\n<p>GPU time depends on data movement as well as floating-point operations. A sequence of individually simple operations may be slow if each writes and rereads large tensors from high-bandwidth memory.</p>\n<p><a href=\"https://arxiv.org/abs/2307.08691\">FlashAttention-2</a> <span class=\"citation\" data-cites=\"dao2023flashattention2\">(Dao 2023)</span> reorganizes exact attention computation to reduce memory traffic and improve work partitioning without materializing the full attention matrix. It does not change the attention objective. Correctness tests compare outputs and gradients with a trusted reference across shapes, masks, dtypes, and edge cases.</p>\n<h2 id=\"parallelism\">Parallelism</h2>\n<p><strong>Data parallelism</strong> replicates the model and gives each replica different examples; gradients are aggregated. <strong>Tensor parallelism</strong> shards large matrix operations within a layer. <strong>Pipeline parallelism</strong> partitions layers and schedules microbatches through stages. <strong>Fully sharded data parallelism</strong> shards parameters, gradients, and optimizer states while gathering what each operation needs.</p>\n<p><a href=\"https://arxiv.org/abs/1909.08053\">Megatron-LM</a> <span class=\"citation\" data-cites=\"shoeybi2019megatron\">(Shoeybi et al. 2019)</span> is a primary reference for model parallelism in large Transformers. <a href=\"https://arxiv.org/abs/1910.02054\">ZeRO</a> <span class=\"citation\" data-cites=\"rajbhandari2020zero\">(Rajbhandari et al. 2020)</span> formalizes staged partitioning of optimizer states, gradients, and parameters.</p>\n<p>No strategy is universally best. The topology, model dimensions, sequence length, device memory, network bandwidth, and failure model determine whether communication or computation dominates.</p>\n<h2 id=\"mixture-of-experts\">Mixture of experts</h2>\n<p>A mixture-of-experts layer routes each token to a subset of feed-forward experts. This increases total parameter capacity without activating every parameter for every token. It introduces router computation, expert load balance, all-to-all communication, capacity limits, and dropped or rerouted tokens.</p>\n<p>The data mixture affects routing because different languages and domains may concentrate on different experts. A systems report should include expert utilization and overflow by data slice, not only average throughput.</p>\n<h2 id=\"checkpointing-and-fault-tolerance\">Checkpointing and fault tolerance</h2>\n<p>A restartable checkpoint includes model weights, optimizer and scheduler state, random-number generator states, data-loader position, tokenizer and mixture versions, and the token count. Saving only weights resumes from a different experiment.</p>\n<p>Checkpoint intervals trade storage and pause cost against lost work. Test recovery deliberately: interrupt a small run, resume it, and compare the next losses and sample order with an uninterrupted control.</p>\n<h2 id=\"studio-profile-and-recover-mini-gpt\">Studio: profile and recover mini-GPT</h2>\n<p>Profile the model from Lecture 2 at two sequence lengths and two precisions. Measure peak memory, tokens per second, model FLOP utilization when available, and time spent in attention, MLP, optimizer, and data loading.</p>\n<p>Then add activation checkpointing or an efficient attention implementation, save a complete checkpoint, terminate the process, resume, and verify the continuation. Submit:</p>\n<ul>\n<li>a predicted versus measured memory table;</li>\n<li>a throughput profile;</li>\n<li>correctness tests for the optimization;</li>\n<li>a compute and storage budget; and</li>\n<li>a recovery runbook with one demonstrated failure.</li>\n</ul>\n<h2 id=\"what-to-remember\">What to remember</h2>\n<ol type=\"1\">\n<li>Optimizer and activation memory can exceed parameter memory.</li>\n<li>Global batch size changes optimization as well as throughput.</li>\n<li>Efficient kernels reduce data movement while preserving the mathematical operation.</li>\n<li>Parallelism strategies shard different resources and introduce different communication.</li>\n<li>MoE capacity includes a routing and load-balance problem tied to the data distribution.</li>\n<li>A checkpoint is complete only when the same experiment can continue from it.</li>\n</ol>\n<h2 class=\"unnumbered\" id=\"references\">References</h2>\n<div id=\"refs\" class=\"references csl-bib-body hanging-indent\" role=\"list\">\n<div id=\"ref-dao2023flashattention2\" class=\"csl-entry\" role=\"listitem\">\nDao, Tri. 2023. <span>“<span>FlashAttention-2</span>: Faster Attention with Better Parallelism and Work Partitioning.”</span> <em>arXiv Preprint arXiv:2307.08691</em>. <a href=\"https://arxiv.org/abs/2307.08691\">https://arxiv.org/abs/2307.08691</a>.\n</div>\n<div id=\"ref-rajbhandari2020zero\" class=\"csl-entry\" role=\"listitem\">\nRajbhandari, Samyam, Jeff Rasley, Olatunji Ruwase, and Yuxiong He. 2020. <span>“<span>ZeRO</span>: Memory Optimizations Toward Training Trillion Parameter Models.”</span> <em>Proceedings of the International Conference for High Performance Computing, Networking, Storage and Analysis</em>. <a href=\"https://doi.org/10.1109/SC41405.2020.00024\">https://doi.org/10.1109/SC41405.2020.00024</a>.\n</div>\n<div id=\"ref-shoeybi2019megatron\" class=\"csl-entry\" role=\"listitem\">\nShoeybi, Mohammad, Mostofa Patwary, Raul Puri, Patrick LeGresley, Jared Casper, and Bryan Catanzaro. 2019. <span>“<span>Megatron-LM</span>: Training Multi-Billion Parameter Language Models Using Model Parallelism.”</span> <em>arXiv Preprint arXiv:1909.08053</em>. <a href=\"https://arxiv.org/abs/1909.08053\">https://arxiv.org/abs/1909.08053</a>.\n</div>\n</div>";
